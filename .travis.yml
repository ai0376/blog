sudo: false
language: go
git:
    depth: 1
install:
    #- go get -v github.com/gohugoio/hugo
    - mkdir $HOME/src
    - cd $HOME/src
    - git clone https://github.com/gohugoio/hugo.git
    - cd hugo
    - git checkout -b hugo0.48 v0.48
    - go install
script:
    - hugo
deploy:
    provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    on:
        branch: master
    local_dir: public
    repo: mjrao/mjrao.github.io
    fqdn: ownwiki.cn
    target_branch: master
    email: mjrao520@gmail.com
    name: mjrao